{
  "name": "@trayb/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check-types": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "cd ../.. && prisma migrate dev --config=packages/db/prisma.config.ts",
    "db:migrate:deploy": "cd ../.. && prisma migrate deploy --config=packages/db/prisma.config.ts",
    "db:studio": "cd ../.. && prisma studio --config=packages/db/prisma.config.ts",
    "db:delete-users": "bun run scripts/delete-users.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^7.0.0",
    "@prisma/client-runtime-utils": "^7.0.0",
    "prisma": "^7.0.0"
  }
}

